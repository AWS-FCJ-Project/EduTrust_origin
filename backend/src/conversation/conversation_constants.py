# Shared constants for conversation handlers.
# Both MongoDB (conversation_handler.py) and DynamoDB (conversation_handler_dynamodb.py)
# handlers use these values so they can evolve independently.

DEFAULT_TITLE = "New Chat"
DEFAULT_LIMIT = 50
